NEXT_PUBLIC_API_URL=http://localhost:4000

# Gate in front of /admin — HTTP Basic Auth checked before the login page loads.
# Leave unset to disable the gate (e.g. local dev); set both to enable it.
ADMIN_GATE_USER=
ADMIN_GATE_PASSWORD=
